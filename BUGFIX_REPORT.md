# Bugfix / Upgrade Report

## Fixed

- Replaced the previous third snippet title with `Jmapelle_hushpuppi`.
- Added `assets/jmapelle_hushpuppi.mp3` and wired it to the preview player.
- Added/kept `assets/cover-jmapelle-hushpuppi.svg` for the new release card.
- Rebuilt the footer into a cleaner, less ambiguous premium music-label footer.
- Removed bulky footer handle text while keeping icon links and POWERED BY EKOUNLOCKED branding.
- Improved popup player behavior with error status text, Esc close support and close button.
- Hardened Supabase role creation with server-side `ensure_profile()` rather than client-side admin role assignment.
- Made `supabase-schema.sql` rerunnable by dropping existing policies before recreating them.
- Fixed the Supabase setup text typo.
- Changed the `nebula-audio` bucket setup to public so preview URLs generated by `getPublicUrl()` actually play on the site.
- Improved dashboard fallback library data, analytics event labels and role-based visibility.
- Rechecked asset references and JavaScript syntax.

## Notes

- The admin password must be created inside Supabase Auth. It is intentionally not hard-coded in public website files.
- Replace remaining demo-loop preview placeholders with licensed snippets before public deployment.

- Updated `assets/cover-jmapelle-hushpuppi.svg` to match the same 1200×1200 Nebula cover-art SVG structure and visual system as the other release SVG assets.

## Upload snippet form reset fix

Fixed the browser error: `can't access property "reset", e.currentTarget is null`.

Cause: the upload and future artist forms were using `e.currentTarget.reset()` after asynchronous Supabase upload/insert operations. In some browsers, the event object's `currentTarget` may be cleared after `await`, so the form reference becomes null.

Fix: the form is now captured immediately at the start of the submit handler using `const form = e.currentTarget;`, then `form.reset()` is called after the async operation. The upload button is also temporarily disabled while Supabase is saving to prevent duplicate submissions.
